//### Variáveis
// Exercício 1 - Crie uma constante chamada myName e atribua a ela o seu nome (Exemplo: Carolina).
const myName = "Priscila";

// Exercício 2 - Crie uma constante chamada birthCity e atribua a ela a sua cidade natal.
const birthCity = "Rio de Janeiro";

// Exercício 3 - Crie uma variável chamada birthYear e atribua a ela o ano em que você nasceu.
let birthYear = 1982;

// Exercício 4 - Utilize o console.log() para imprimir as constantes e variáveis que você criou.
console.log (myName);
console.log (birthCity);
console.log (birthYear);

// Exercício 5 - Altere o valor atribuído à variável birthYear para 2030. Faça um console.log(birthYear) novamente para ver o que acontece!
birthYear = 2030;
console.log(birthYear);

// Exercício 6 - Altere o valor atribuído à constante birthCity. Faça um console.log(birthCity) novamente! Você saberia explicar por que recebemos uma mensagem de erro? 🤔
// birthCity = "São Paulo";
// console.log (birthCity);


//### Tipos Primitivos, Tipagem Dinâmica e Operações Aritméticas
let patientId = 50;
let isEnrolled = true;
const patientInfo = {
  firstName: 'Ana',
  lastName: 'Santos',
};
const patientEmail = 'ana@email.com';

//Exercicio 1 - Experimente executar o comando console.log(typeof patientAge) e veja o que acontece! 
console.log(typeof patientAge);
// Ué...mas não declaramos essa variável, não é mesmo? É exatamente por esse motivo que o seu tipo é undefined, como você pôde observar no retorno do console.log(typeof patientAge). 

//Exercicio 2 - Experimente também trocar o valor de patientId = 50 para patientId = '50'. Execute novamente um console.log() para imprimir o tipo dessa variável após a modificação. 
patientId = "50";
console.log (patientId);
//Você verá que o retorno agora é uma string pois colocamos o número 50 dentro das aspas. O JavaScript interpreta como string tudo o que estiver entre aspas.

//Exercicio 3 - Crie uma constante chamada base e uma variável chamada height e atribua os seus respectivos valores: 5 e 8.
const base = 5;
let height = 8;

//Exercicio 4 - Crie uma constante chamada area e atribua a ela o resultado da multiplicação da base pela altura. Dica: lembre-se de usar sempre o console.log() para imprimir as variáveis e checar os resultados das operações!
const area = base * height;
console.log (area);

//Exercicio 5 - Crie uma constante chamada perimeter e atribua a ela a soma de todos os lados do retângulo.
const perimeter = base * 2 + height * 2;
console.log (perimeter);


//## Condições If e Else
// Exercicio 1 - Crie uma constante que receba a nota de uma pessoa candidata em um desafio técnico, e atribua a ela um valor entre 1 e 100;
const notaTesteTecnico = 80;

// Exercicio 2 - Implemente uma lógica que verifique se a pessoa candidata foi aprovada, reprovada ou se ela está na lista de espera. Para isso, considere as seguintes informações:
// Exercicio 2.1 - Se a nota for maior ou igual a 80, imprima "Parabéns, você foi aprovada(o)!"
// Exercicio 2.2 - Se a nota for menor que 80 e maior ou igual a 60, imprima "Você está na nossa lista de espera"
// Exercicio 2.3 - Se a nota for menor que 60, imprima "Você foi reprovada(o)"
if (notaTesteTecnico >= 80) {
    console.log ("Parabéns, você foi aprovada(o)!")
} else if (notaTesteTecnico >= 60 < 80) {
    console.log ("Você está na nossa lista de espera")
} else {
    console.log ("Você foi reprovada(o)")
};

// Exercicio 3 - Crie uma estrutura condicional utilizando o if, else if e else para criar o seu algoritmo, e os operadores lógicos que se aplicam a cada situação.
// Exercicio 4 - Altere o valor da nota para verificar se as condições que você implementou funcionam;
//notaTesteTecnico = 30;
if (notaTesteTecnico >= 80) {
    console.log ("Parabéns, você foi aprovada(o)!")
} else if (notaTesteTecnico >= 60 < 80) {
    console.log ("Você está na nossa lista de espera")
} else {
    console.log ("Você foi reprovada(o)")
};


//## Operadores lógicos - Operador AND
// Exercicio 1 - Crie uma constante chamada "currentHour" que recebe um número entre 4 e 24 de sua escolha, para representar as horas do dia.
const currentHour = 20;

// Exercicio 2 - Crie uma variável chamada "message" que, inicialmente, é uma string vazia.
let message;

// Exercicio 3 - Implemente condicionais para que:
// Exercicio 3.1 - Se o horário for maior ou igual a 22, insira "Não deveríamos comer nada, é hora de dormir" na variável "message".
// Exercicio 3.2 - Se o horário for maior ou igual a 18 e menor que 22, insira "Rango da noite, vamos jantar :D" na variável "message".
// Exercicio 3.3 - Se o horário for maior ou igual a 14 e menor que 18, insira "Vamos fazer um bolo pro café da tarde?" na variável "message".
// Exercicio 3.4 - Se o horário estiver entre 11 e 14, insira "Hora do almoço!!!" na variável "message".
// Exercicio 3.5 - Se o horário estiver entre 4 e 11, insira "Hmmm, cheiro de café recém passado" na variável "message".
// Exercicio 3.6 - Agora imprima a variável message fora das suas condições.
if (currentHour >= 22) {
    console.log (message) = "Não deveríamos comer nada, é hora de dormir"
} else if (currentHour >= 18 < 22) {
    message = "Rango da noite, vamos jantar :D"
} else if (currentHour >= 14 < 18) {
    message = "Vamos fazer um bolo pro café da tarde?"
} else if (currentHour >= 11 < 14) {
    message = "Hora do almoço!!!"
} else {
    message = "Hmmm, cheiro de café recém passado"
}
console.log (message)


//## Operadores lógicos - Operador OR
// Exercicio 1 - Crie uma variável chamada "weekDay" que recebe a string "quarta-feira".
let weekDay = "quarta-feira";

// Exercicio 2 - Implemente condicionais para que:
// Exercicio 2.1 - Se nossa variável "weekDay" for "segunda-feira", "terça-feira", "quarta-feira", "quinta-feira" ou "sexta-feira", imprima "Oba, mais um dia de aprendizado na Trybe >:D".
// Exercicio 2.2 - Se for algum dia de fim de semana, imprima "FINALMENTE, descanso merecido UwU".
if (weekDay = "segunda-feira" || "terça-feira" || "quarta-feira" || "quinta-feira" || "sexta-feira") {
    console.log ("Oba, mais um dia de aprendizado na Trybe >:D")
} else {
    console.log ("FINALMENTE, descanso merecido UwU")
}


//## Operadores lógicos - Switch e Case
// Exercicio 1 - Crie uma variável para armazenar o estado da pessoa candidata no processo seletivo, que pode ser: 'aprovada', 'lista' ou 'reprovada';
let candidato = "lista";

// Exercicio 2 - Crie uma estrutura condicional com o switch/case que irá imprimir as mensagens do exercício anterior se o estado da pessoa candidata for 'aprovada', 'lista' ou 'reprovada'. Como default, imprima a mensagem de 'não se aplica'.
switch (candidato) {
    case "aprovado":
    console.log ("Parabéns, você foi aprovada(o)!")
    break;
    case "lista":
    console.log ("Você está na nossa lista de espera");
    break;
    case "reprovada":
    console.log ("Você foi reprovada(o)");
    break;
}
